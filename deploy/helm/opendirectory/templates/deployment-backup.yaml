apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "opendirectory.fullname" . }}-backup
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: backup
spec:
  replicas: {{ .Values.backup.replicaCount }}
  selector:
    matchLabels:
      {{- include "opendirectory.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: backup
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "opendirectory.labels" . | nindent 8 }}
        app.kubernetes.io/component: backup
      annotations:
        {{- include "opendirectory.prometheusAnnotations" . | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "opendirectory.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- include "opendirectory.podAntiAffinity" . | nindent 6 }}
      containers:
        - name: backup
          image: {{ include "opendirectory.image" (dict "root" . "name" .Values.backup.image.name) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.backup.service.port }}
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "opendirectory.fullname" . }}-config
            - secretRef:
                name: {{ include "opendirectory.fullname" . }}-secret
          env:
            - name: PORT
              value: {{ .Values.backup.service.port | quote }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.backup.resources | nindent 12 }}
          {{- if .Values.persistence.enabled }}
          volumeMounts:
            - name: backup-storage
              mountPath: /backups
          {{- end }}
      {{- if .Values.persistence.enabled }}
      volumes:
        - name: backup-storage
          persistentVolumeClaim:
            claimName: {{ include "opendirectory.fullname" . }}-backup-storage
      {{- end }}
