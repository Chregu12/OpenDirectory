apiVersion: v1
kind: Service
metadata:
  name: antivirus-protection
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: antivirus
spec:
  type: {{ .Values.antivirus.service.type }}
  ports:
    - port: {{ .Values.antivirus.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: antivirus
