apiVersion: v1
kind: Service
metadata:
  name: app-store
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: appstore
spec:
  type: {{ .Values.appStore.service.type }}
  ports:
    - port: {{ .Values.appStore.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: appstore
