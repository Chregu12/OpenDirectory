apiVersion: v1
kind: Service
metadata:
  name: certificate-network
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: cert-network
spec:
  type: {{ .Values.certNetwork.service.type }}
  ports:
    - port: {{ .Values.certNetwork.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: cert-network
