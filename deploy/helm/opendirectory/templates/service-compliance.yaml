apiVersion: v1
kind: Service
metadata:
  name: compliance-engine
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: compliance
spec:
  type: {{ .Values.complianceEngine.service.type }}
  ports:
    - port: {{ .Values.complianceEngine.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: compliance
