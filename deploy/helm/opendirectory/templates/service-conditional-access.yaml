apiVersion: v1
kind: Service
metadata:
  name: conditional-access
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: conditional-access
spec:
  type: {{ .Values.conditionalAccess.service.type }}
  ports:
    - port: {{ .Values.conditionalAccess.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: conditional-access
