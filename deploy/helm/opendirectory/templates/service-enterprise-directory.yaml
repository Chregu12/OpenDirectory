apiVersion: v1
kind: Service
metadata:
  name: enterprise-directory
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: enterprise-directory
spec:
  type: {{ .Values.enterpriseDirectory.service.type }}
  ports:
    - port: {{ .Values.enterpriseDirectory.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: enterprise-directory
