apiVersion: v1
kind: Service
metadata:
  name: apple-mdm
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: mdm
spec:
  type: {{ .Values.mdm.service.type }}
  ports:
    - port: {{ .Values.mdm.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: mdm
