apiVersion: v1
kind: Service
metadata:
  name: mobile-management
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: mobile
spec:
  type: {{ .Values.mobileMgmt.service.type }}
  ports:
    - port: {{ .Values.mobileMgmt.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: mobile
