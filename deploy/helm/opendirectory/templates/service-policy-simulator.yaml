apiVersion: v1
kind: Service
metadata:
  name: policy-simulator
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: policy-simulator
spec:
  type: {{ .Values.policySimulator.service.type }}
  ports:
    - port: {{ .Values.policySimulator.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: policy-simulator
