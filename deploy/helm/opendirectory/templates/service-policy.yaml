apiVersion: v1
kind: Service
metadata:
  name: {{ include "opendirectory.fullname" . }}-policy
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: policy
spec:
  type: {{ .Values.policy.service.type }}
  ports:
    - port: {{ .Values.policy.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: policy
