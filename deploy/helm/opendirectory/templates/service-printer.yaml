apiVersion: v1
kind: Service
metadata:
  name: printer-service
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: printer
spec:
  type: {{ .Values.printer.service.type }}
  ports:
    - port: {{ .Values.printer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: printer
