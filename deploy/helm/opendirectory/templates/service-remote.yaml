apiVersion: v1
kind: Service
metadata:
  name: remote-control
  namespace: {{ include "opendirectory.namespace" . }}
  labels:
    {{- include "opendirectory.labels" . | nindent 4 }}
    app.kubernetes.io/component: remote
spec:
  type: {{ .Values.remoteControl.service.type }}
  ports:
    - port: {{ .Values.remoteControl.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendirectory.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: remote
